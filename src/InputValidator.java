/**
 * InputValidator.java — Member 7
 *
 * Central place for ALL input validation logic used across the system.
 * The GUI calls these methods before accepting any user input.
 * DO NOT add Scanner or System.out here — these methods only check
 * data and return true/false or throw an exception with a clear message.
 *
 * TODO (Member 7):
 *   Add static methods such as:
 *     1. isValidName(String name)              → false if empty/blank
 *     2. isValidAge(int age)                   → true if 1–120
 *     3. isValidWeight(double weight)          → true if > 0 (and < 500 as sanity check)
 *     4. isValidHeight(double height)          → true if > 0 (and < 300 as sanity check)
 *     5. isValidSets(int sets)                 → true if ≥ 1
 *     6. isValidReps(int reps)                 → true if ≥ 1
 *     7. isValidDuration(int duration)         → true if ≥ 1
 *
 *   Each method should be simple and return a boolean.
 *   Example:
 *     public static boolean isValidAge(int age) {
 *         return age > 0 && age <= 120;
 *     }
 *
 *   Once done, the Leader will plug these into WorkoutGUI.java to replace
 *   the temporary manual checks currently inside the GUI code.
 */
public class InputValidator {

    // TODO: Add your validation methods here

}
